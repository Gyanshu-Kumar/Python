export interface FindNewsDto {
    fetchAfter?: Date,
    sortBy?: string,
    limit?: number,
    offset?: number,
    searchQuery?: string,
    categoryId?: number[],
    subCategoryId?: number[],
    tokenId?: number[]
}